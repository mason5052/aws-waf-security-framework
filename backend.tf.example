# AWS WAF Security Framework - Remote State Backend
# Copy this file to backend.tf and configure for your environment

terraform {
  backend "s3" {
    bucket         = "my-terraform-state"
    key            = "waf/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "terraform-lock"
  }
}
